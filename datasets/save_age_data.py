import os
import cv2
import numpy as np
from tqdm import tqdm
# Folder containing images


# Verify the shape of the resulting arrays
print(images.shape)  # (num_samples, height, width, num_channels)
print(ages.shape)    # (num_samples,)
